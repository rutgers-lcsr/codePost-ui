// @ts-nocheck
/* tslint:disable */
/* eslint-disable */
/**
 * codePost API
 *  An API for administrators to mine course data and automate common tasks.  ## Quick Start  ### Installation  ```bash pip install git+https://github.com/rutgers-lcsr/codepost-python.git ```  ### Basic Usage (Python SDK)  ```python from codepost import CodePost, Comment  client = CodePost(api_key=\"YOUR_API_KEY\")  # Create a course with kwargs shorthand course = client.courses.create(name=\"CS101\", period=\"Fall 2026\")  # Create an assignment assignment = client.assignments.create(     name=\"Homework 1\",     course=course.id,     points=100, )  # Create submission + upload files in one call submission = client.submissions.create_with_files(     assignment=assignment.id,     students=[\"student@example.com\"],     files=[         {\"name\": \"main.py\", \"data\": \"print(\'hello\')\", \"extension\": \".py\"},     ], )  # Add inline feedback file_id = client.submissions.retrieve(submission.id).files[0].id client.comments.create(     Comment(         file=file_id,         text=\"Nice work\",         start_line=1,         end_line=1,         point_delta=1,     ) )  # Finalize and export grades client.submissions.bulk_finalize([submission.id], grader=\"ta@example.com\") rows = client.assignments.submissions.export_grades(assignment_id=assignment.id) print(rows[0]) ```  See the [Python SDK Repository](https://github.com/rutgers-lcsr/codepost-python) for more examples.
 *
 * The version of the OpenAPI document: 3.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import type {
  PaginatedSystemPromptVariantList,
  PatchedSystemPromptVariant,
  SystemPromptVariant,
} from '../models/index';

export interface ActivateCreateRequest {
  id: number;
}

export interface AutoImproveCreateRequest {
  promptType: string;
}

export interface CloneCreateRequest {
  id: number;
}

export interface CreateRequest {
  systemPromptVariant: Omit<SystemPromptVariant, 'id' | 'version' | 'createdBy' | 'created' | 'modified'>;
}

export interface DestroyRequest {
  id: number;
}

export interface ListRequest {
  page?: number;
  pageSize?: number;
}

export interface PartialUpdateRequest {
  id: number;
  patchedSystemPromptVariant?: Omit<
    PatchedSystemPromptVariant,
    'id' | 'version' | 'createdBy' | 'created' | 'modified'
  >;
}

export interface RetrieveRequest {
  id: number;
}

export interface SettingsUpdateUpdateRequest {
  requestBody?: { [key: string]: any | undefined };
}

export interface StatsRetrieveRequest {
  id: number;
}

export interface UpdateRequest {
  id: number;
  systemPromptVariant: Omit<SystemPromptVariant, 'id' | 'version' | 'createdBy' | 'created' | 'modified'>;
}

/**
 *
 */
export class PromptVariantsApi extends runtime.BaseAPI {
  /**
   * Set this variant as the active prompt for its type. The previously active variant (if any) is moved to \'retired\'.
   */
  async activateCreateRaw(
    requestParameters: ActivateCreateRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<SystemPromptVariant>> {
    if (requestParameters['id'] == null) {
      throw new runtime.RequiredError(
        'id',
        'Required parameter "id" was null or undefined when calling activateCreate().',
      );
    }

    const queryParameters: any = {};

    const headerParameters: runtime.HTTPHeaders = {};

    if (
      this.configuration &&
      (this.configuration.username !== undefined || this.configuration.password !== undefined)
    ) {
      headerParameters['Authorization'] =
        'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
    }
    if (this.configuration && this.configuration.apiKey) {
      headerParameters['Authorization'] = await this.configuration.apiKey('Authorization'); // tokenAuth authentication
    }

    if (this.configuration && this.configuration.apiKey) {
      headerParameters['Authorization'] = await this.configuration.apiKey('Authorization'); // courseKeyAuth authentication
    }

    let urlPath = `/promptVariants/{id}/activate/`;
    urlPath = urlPath.replace(`{${'id'}}`, encodeURIComponent(String(requestParameters['id'])));

    const response = await this.request(
      {
        path: urlPath,
        method: 'POST',
        headers: headerParameters,
        query: queryParameters,
      },
      initOverrides,
    );

    return new runtime.JSONApiResponse(response);
  }

  /**
   * Set this variant as the active prompt for its type. The previously active variant (if any) is moved to \'retired\'.
   */
  async activateCreate(
    requestParameters: ActivateCreateRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<SystemPromptVariant> {
    const response = await this.activateCreateRaw(requestParameters, initOverrides);
    return await response.value();
  }

  /**
   * Analyze feedback for a prompt type and use AI to generate an improved variant. Creates a new draft child of the current active variant.
   */
  async autoImproveCreateRaw(
    requestParameters: AutoImproveCreateRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<SystemPromptVariant>> {
    if (requestParameters['promptType'] == null) {
      throw new runtime.RequiredError(
        'promptType',
        'Required parameter "promptType" was null or undefined when calling autoImproveCreate().',
      );
    }

    const queryParameters: any = {};

    if (requestParameters['promptType'] != null) {
      queryParameters['promptType'] = requestParameters['promptType'];
    }

    const headerParameters: runtime.HTTPHeaders = {};

    if (
      this.configuration &&
      (this.configuration.username !== undefined || this.configuration.password !== undefined)
    ) {
      headerParameters['Authorization'] =
        'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
    }
    if (this.configuration && this.configuration.apiKey) {
      headerParameters['Authorization'] = await this.configuration.apiKey('Authorization'); // tokenAuth authentication
    }

    if (this.configuration && this.configuration.apiKey) {
      headerParameters['Authorization'] = await this.configuration.apiKey('Authorization'); // courseKeyAuth authentication
    }

    let urlPath = `/promptVariants/auto-improve/`;

    const response = await this.request(
      {
        path: urlPath,
        method: 'POST',
        headers: headerParameters,
        query: queryParameters,
      },
      initOverrides,
    );

    return new runtime.JSONApiResponse(response);
  }

  /**
   * Analyze feedback for a prompt type and use AI to generate an improved variant. Creates a new draft child of the current active variant.
   */
  async autoImproveCreate(
    requestParameters: AutoImproveCreateRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<SystemPromptVariant> {
    const response = await this.autoImproveCreateRaw(requestParameters, initOverrides);
    return await response.value();
  }

  /**
   * Clone this variant as a new draft for editing.
   */
  async cloneCreateRaw(
    requestParameters: CloneCreateRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<SystemPromptVariant>> {
    if (requestParameters['id'] == null) {
      throw new runtime.RequiredError(
        'id',
        'Required parameter "id" was null or undefined when calling cloneCreate().',
      );
    }

    const queryParameters: any = {};

    const headerParameters: runtime.HTTPHeaders = {};

    if (
      this.configuration &&
      (this.configuration.username !== undefined || this.configuration.password !== undefined)
    ) {
      headerParameters['Authorization'] =
        'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
    }
    if (this.configuration && this.configuration.apiKey) {
      headerParameters['Authorization'] = await this.configuration.apiKey('Authorization'); // tokenAuth authentication
    }

    if (this.configuration && this.configuration.apiKey) {
      headerParameters['Authorization'] = await this.configuration.apiKey('Authorization'); // courseKeyAuth authentication
    }

    let urlPath = `/promptVariants/{id}/clone/`;
    urlPath = urlPath.replace(`{${'id'}}`, encodeURIComponent(String(requestParameters['id'])));

    const response = await this.request(
      {
        path: urlPath,
        method: 'POST',
        headers: headerParameters,
        query: queryParameters,
      },
      initOverrides,
    );

    return new runtime.JSONApiResponse(response);
  }

  /**
   * Clone this variant as a new draft for editing.
   */
  async cloneCreate(
    requestParameters: CloneCreateRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<SystemPromptVariant> {
    const response = await this.cloneCreateRaw(requestParameters, initOverrides);
    return await response.value();
  }

  /**
   * CRUD for platform-global AI system prompt variants.  Superuser-only.  Use the ``activate`` action to promote a variant as the active prompt for its type (retiring the previous active variant).  list: List all prompt variants (superuser only). Supports ?promptType= filter.  retrieve: Return a single variant.  create: Create a new variant (status defaults to \'draft\').  update / partial_update: Edit a variant. Cannot edit an \'active\' variant directly — clone it first, edit the clone, then activate the clone.  delete: Delete a variant. Active variants cannot be deleted.
   */
  async createRaw(
    requestParameters: CreateRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<SystemPromptVariant>> {
    if (requestParameters['systemPromptVariant'] == null) {
      throw new runtime.RequiredError(
        'systemPromptVariant',
        'Required parameter "systemPromptVariant" was null or undefined when calling create().',
      );
    }

    const queryParameters: any = {};

    const headerParameters: runtime.HTTPHeaders = {};

    headerParameters['Content-Type'] = 'application/json';

    if (
      this.configuration &&
      (this.configuration.username !== undefined || this.configuration.password !== undefined)
    ) {
      headerParameters['Authorization'] =
        'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
    }
    if (this.configuration && this.configuration.apiKey) {
      headerParameters['Authorization'] = await this.configuration.apiKey('Authorization'); // tokenAuth authentication
    }

    if (this.configuration && this.configuration.apiKey) {
      headerParameters['Authorization'] = await this.configuration.apiKey('Authorization'); // courseKeyAuth authentication
    }

    let urlPath = `/promptVariants/`;

    const response = await this.request(
      {
        path: urlPath,
        method: 'POST',
        headers: headerParameters,
        query: queryParameters,
        body: requestParameters['systemPromptVariant'],
      },
      initOverrides,
    );

    return new runtime.JSONApiResponse(response);
  }

  /**
   * CRUD for platform-global AI system prompt variants.  Superuser-only.  Use the ``activate`` action to promote a variant as the active prompt for its type (retiring the previous active variant).  list: List all prompt variants (superuser only). Supports ?promptType= filter.  retrieve: Return a single variant.  create: Create a new variant (status defaults to \'draft\').  update / partial_update: Edit a variant. Cannot edit an \'active\' variant directly — clone it first, edit the clone, then activate the clone.  delete: Delete a variant. Active variants cannot be deleted.
   */
  async create(
    requestParameters: CreateRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<SystemPromptVariant> {
    const response = await this.createRaw(requestParameters, initOverrides);
    return await response.value();
  }

  /**
   * CRUD for platform-global AI system prompt variants.  Superuser-only.  Use the ``activate`` action to promote a variant as the active prompt for its type (retiring the previous active variant).  list: List all prompt variants (superuser only). Supports ?promptType= filter.  retrieve: Return a single variant.  create: Create a new variant (status defaults to \'draft\').  update / partial_update: Edit a variant. Cannot edit an \'active\' variant directly — clone it first, edit the clone, then activate the clone.  delete: Delete a variant. Active variants cannot be deleted.
   */
  async destroyRaw(
    requestParameters: DestroyRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<void>> {
    if (requestParameters['id'] == null) {
      throw new runtime.RequiredError('id', 'Required parameter "id" was null or undefined when calling destroy().');
    }

    const queryParameters: any = {};

    const headerParameters: runtime.HTTPHeaders = {};

    if (
      this.configuration &&
      (this.configuration.username !== undefined || this.configuration.password !== undefined)
    ) {
      headerParameters['Authorization'] =
        'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
    }
    if (this.configuration && this.configuration.apiKey) {
      headerParameters['Authorization'] = await this.configuration.apiKey('Authorization'); // tokenAuth authentication
    }

    if (this.configuration && this.configuration.apiKey) {
      headerParameters['Authorization'] = await this.configuration.apiKey('Authorization'); // courseKeyAuth authentication
    }

    let urlPath = `/promptVariants/{id}/`;
    urlPath = urlPath.replace(`{${'id'}}`, encodeURIComponent(String(requestParameters['id'])));

    const response = await this.request(
      {
        path: urlPath,
        method: 'DELETE',
        headers: headerParameters,
        query: queryParameters,
      },
      initOverrides,
    );

    return new runtime.VoidApiResponse(response);
  }

  /**
   * CRUD for platform-global AI system prompt variants.  Superuser-only.  Use the ``activate`` action to promote a variant as the active prompt for its type (retiring the previous active variant).  list: List all prompt variants (superuser only). Supports ?promptType= filter.  retrieve: Return a single variant.  create: Create a new variant (status defaults to \'draft\').  update / partial_update: Edit a variant. Cannot edit an \'active\' variant directly — clone it first, edit the clone, then activate the clone.  delete: Delete a variant. Active variants cannot be deleted.
   */
  async destroy(
    requestParameters: DestroyRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<void> {
    await this.destroyRaw(requestParameters, initOverrides);
  }

  /**
   * CRUD for platform-global AI system prompt variants.  Superuser-only.  Use the ``activate`` action to promote a variant as the active prompt for its type (retiring the previous active variant).  list: List all prompt variants (superuser only). Supports ?promptType= filter.  retrieve: Return a single variant.  create: Create a new variant (status defaults to \'draft\').  update / partial_update: Edit a variant. Cannot edit an \'active\' variant directly — clone it first, edit the clone, then activate the clone.  delete: Delete a variant. Active variants cannot be deleted.
   */
  async listRaw(
    requestParameters: ListRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<PaginatedSystemPromptVariantList>> {
    const queryParameters: any = {};

    if (requestParameters['page'] != null) {
      queryParameters['page'] = requestParameters['page'];
    }

    if (requestParameters['pageSize'] != null) {
      queryParameters['page_size'] = requestParameters['pageSize'];
    }

    const headerParameters: runtime.HTTPHeaders = {};

    if (
      this.configuration &&
      (this.configuration.username !== undefined || this.configuration.password !== undefined)
    ) {
      headerParameters['Authorization'] =
        'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
    }
    if (this.configuration && this.configuration.apiKey) {
      headerParameters['Authorization'] = await this.configuration.apiKey('Authorization'); // tokenAuth authentication
    }

    if (this.configuration && this.configuration.apiKey) {
      headerParameters['Authorization'] = await this.configuration.apiKey('Authorization'); // courseKeyAuth authentication
    }

    let urlPath = `/promptVariants/`;

    const response = await this.request(
      {
        path: urlPath,
        method: 'GET',
        headers: headerParameters,
        query: queryParameters,
      },
      initOverrides,
    );

    return new runtime.JSONApiResponse(response);
  }

  /**
   * CRUD for platform-global AI system prompt variants.  Superuser-only.  Use the ``activate`` action to promote a variant as the active prompt for its type (retiring the previous active variant).  list: List all prompt variants (superuser only). Supports ?promptType= filter.  retrieve: Return a single variant.  create: Create a new variant (status defaults to \'draft\').  update / partial_update: Edit a variant. Cannot edit an \'active\' variant directly — clone it first, edit the clone, then activate the clone.  delete: Delete a variant. Active variants cannot be deleted.
   */
  async list(
    requestParameters: ListRequest = {},
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<PaginatedSystemPromptVariantList> {
    const response = await this.listRaw(requestParameters, initOverrides);
    return await response.value();
  }

  /**
   * CRUD for platform-global AI system prompt variants.  Superuser-only.  Use the ``activate`` action to promote a variant as the active prompt for its type (retiring the previous active variant).  list: List all prompt variants (superuser only). Supports ?promptType= filter.  retrieve: Return a single variant.  create: Create a new variant (status defaults to \'draft\').  update / partial_update: Edit a variant. Cannot edit an \'active\' variant directly — clone it first, edit the clone, then activate the clone.  delete: Delete a variant. Active variants cannot be deleted.
   */
  async partialUpdateRaw(
    requestParameters: PartialUpdateRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<SystemPromptVariant>> {
    if (requestParameters['id'] == null) {
      throw new runtime.RequiredError(
        'id',
        'Required parameter "id" was null or undefined when calling partialUpdate().',
      );
    }

    const queryParameters: any = {};

    const headerParameters: runtime.HTTPHeaders = {};

    headerParameters['Content-Type'] = 'application/json';

    if (
      this.configuration &&
      (this.configuration.username !== undefined || this.configuration.password !== undefined)
    ) {
      headerParameters['Authorization'] =
        'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
    }
    if (this.configuration && this.configuration.apiKey) {
      headerParameters['Authorization'] = await this.configuration.apiKey('Authorization'); // tokenAuth authentication
    }

    if (this.configuration && this.configuration.apiKey) {
      headerParameters['Authorization'] = await this.configuration.apiKey('Authorization'); // courseKeyAuth authentication
    }

    let urlPath = `/promptVariants/{id}/`;
    urlPath = urlPath.replace(`{${'id'}}`, encodeURIComponent(String(requestParameters['id'])));

    const response = await this.request(
      {
        path: urlPath,
        method: 'PATCH',
        headers: headerParameters,
        query: queryParameters,
        body: requestParameters['patchedSystemPromptVariant'],
      },
      initOverrides,
    );

    return new runtime.JSONApiResponse(response);
  }

  /**
   * CRUD for platform-global AI system prompt variants.  Superuser-only.  Use the ``activate`` action to promote a variant as the active prompt for its type (retiring the previous active variant).  list: List all prompt variants (superuser only). Supports ?promptType= filter.  retrieve: Return a single variant.  create: Create a new variant (status defaults to \'draft\').  update / partial_update: Edit a variant. Cannot edit an \'active\' variant directly — clone it first, edit the clone, then activate the clone.  delete: Delete a variant. Active variants cannot be deleted.
   */
  async partialUpdate(
    requestParameters: PartialUpdateRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<SystemPromptVariant> {
    const response = await this.partialUpdateRaw(requestParameters, initOverrides);
    return await response.value();
  }

  /**
   * CRUD for platform-global AI system prompt variants.  Superuser-only.  Use the ``activate`` action to promote a variant as the active prompt for its type (retiring the previous active variant).  list: List all prompt variants (superuser only). Supports ?promptType= filter.  retrieve: Return a single variant.  create: Create a new variant (status defaults to \'draft\').  update / partial_update: Edit a variant. Cannot edit an \'active\' variant directly — clone it first, edit the clone, then activate the clone.  delete: Delete a variant. Active variants cannot be deleted.
   */
  async retrieveRaw(
    requestParameters: RetrieveRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<SystemPromptVariant>> {
    if (requestParameters['id'] == null) {
      throw new runtime.RequiredError('id', 'Required parameter "id" was null or undefined when calling retrieve().');
    }

    const queryParameters: any = {};

    const headerParameters: runtime.HTTPHeaders = {};

    if (
      this.configuration &&
      (this.configuration.username !== undefined || this.configuration.password !== undefined)
    ) {
      headerParameters['Authorization'] =
        'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
    }
    if (this.configuration && this.configuration.apiKey) {
      headerParameters['Authorization'] = await this.configuration.apiKey('Authorization'); // tokenAuth authentication
    }

    if (this.configuration && this.configuration.apiKey) {
      headerParameters['Authorization'] = await this.configuration.apiKey('Authorization'); // courseKeyAuth authentication
    }

    let urlPath = `/promptVariants/{id}/`;
    urlPath = urlPath.replace(`{${'id'}}`, encodeURIComponent(String(requestParameters['id'])));

    const response = await this.request(
      {
        path: urlPath,
        method: 'GET',
        headers: headerParameters,
        query: queryParameters,
      },
      initOverrides,
    );

    return new runtime.JSONApiResponse(response);
  }

  /**
   * CRUD for platform-global AI system prompt variants.  Superuser-only.  Use the ``activate`` action to promote a variant as the active prompt for its type (retiring the previous active variant).  list: List all prompt variants (superuser only). Supports ?promptType= filter.  retrieve: Return a single variant.  create: Create a new variant (status defaults to \'draft\').  update / partial_update: Edit a variant. Cannot edit an \'active\' variant directly — clone it first, edit the clone, then activate the clone.  delete: Delete a variant. Active variants cannot be deleted.
   */
  async retrieve(
    requestParameters: RetrieveRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<SystemPromptVariant> {
    const response = await this.retrieveRaw(requestParameters, initOverrides);
    return await response.value();
  }

  /**
   * Get the current Prompt Lab auto-improvement settings.
   */
  async settingsRetrieveRaw(
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<{ [key: string]: any | undefined }>> {
    const queryParameters: any = {};

    const headerParameters: runtime.HTTPHeaders = {};

    if (
      this.configuration &&
      (this.configuration.username !== undefined || this.configuration.password !== undefined)
    ) {
      headerParameters['Authorization'] =
        'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
    }
    if (this.configuration && this.configuration.apiKey) {
      headerParameters['Authorization'] = await this.configuration.apiKey('Authorization'); // tokenAuth authentication
    }

    if (this.configuration && this.configuration.apiKey) {
      headerParameters['Authorization'] = await this.configuration.apiKey('Authorization'); // courseKeyAuth authentication
    }

    let urlPath = `/promptVariants/settings/`;

    const response = await this.request(
      {
        path: urlPath,
        method: 'GET',
        headers: headerParameters,
        query: queryParameters,
      },
      initOverrides,
    );

    return new runtime.JSONApiResponse<any>(response);
  }

  /**
   * Get the current Prompt Lab auto-improvement settings.
   */
  async settingsRetrieve(
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<{ [key: string]: any | undefined }> {
    const response = await this.settingsRetrieveRaw(initOverrides);
    return await response.value();
  }

  /**
   * Update Prompt Lab auto-improvement settings.
   */
  async settingsUpdateUpdateRaw(
    requestParameters: SettingsUpdateUpdateRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<{ [key: string]: any | undefined }>> {
    const queryParameters: any = {};

    const headerParameters: runtime.HTTPHeaders = {};

    headerParameters['Content-Type'] = 'application/json';

    if (
      this.configuration &&
      (this.configuration.username !== undefined || this.configuration.password !== undefined)
    ) {
      headerParameters['Authorization'] =
        'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
    }
    if (this.configuration && this.configuration.apiKey) {
      headerParameters['Authorization'] = await this.configuration.apiKey('Authorization'); // tokenAuth authentication
    }

    if (this.configuration && this.configuration.apiKey) {
      headerParameters['Authorization'] = await this.configuration.apiKey('Authorization'); // courseKeyAuth authentication
    }

    let urlPath = `/promptVariants/settings/update/`;

    const response = await this.request(
      {
        path: urlPath,
        method: 'PUT',
        headers: headerParameters,
        query: queryParameters,
        body: requestParameters['requestBody'],
      },
      initOverrides,
    );

    return new runtime.JSONApiResponse<any>(response);
  }

  /**
   * Update Prompt Lab auto-improvement settings.
   */
  async settingsUpdateUpdate(
    requestParameters: SettingsUpdateUpdateRequest = {},
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<{ [key: string]: any | undefined }> {
    const response = await this.settingsUpdateUpdateRaw(requestParameters, initOverrides);
    return await response.value();
  }

  /**
   * Get performance stats for this prompt variant: behavioral metrics (acceptance/rejection/edit rates, time to decide) and explicit feedback (thumbs up/down).
   */
  async statsRetrieveRaw(
    requestParameters: StatsRetrieveRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<{ [key: string]: any | undefined }>> {
    if (requestParameters['id'] == null) {
      throw new runtime.RequiredError(
        'id',
        'Required parameter "id" was null or undefined when calling statsRetrieve().',
      );
    }

    const queryParameters: any = {};

    const headerParameters: runtime.HTTPHeaders = {};

    if (
      this.configuration &&
      (this.configuration.username !== undefined || this.configuration.password !== undefined)
    ) {
      headerParameters['Authorization'] =
        'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
    }
    if (this.configuration && this.configuration.apiKey) {
      headerParameters['Authorization'] = await this.configuration.apiKey('Authorization'); // tokenAuth authentication
    }

    if (this.configuration && this.configuration.apiKey) {
      headerParameters['Authorization'] = await this.configuration.apiKey('Authorization'); // courseKeyAuth authentication
    }

    let urlPath = `/promptVariants/{id}/stats/`;
    urlPath = urlPath.replace(`{${'id'}}`, encodeURIComponent(String(requestParameters['id'])));

    const response = await this.request(
      {
        path: urlPath,
        method: 'GET',
        headers: headerParameters,
        query: queryParameters,
      },
      initOverrides,
    );

    return new runtime.JSONApiResponse<any>(response);
  }

  /**
   * Get performance stats for this prompt variant: behavioral metrics (acceptance/rejection/edit rates, time to decide) and explicit feedback (thumbs up/down).
   */
  async statsRetrieve(
    requestParameters: StatsRetrieveRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<{ [key: string]: any | undefined }> {
    const response = await this.statsRetrieveRaw(requestParameters, initOverrides);
    return await response.value();
  }

  /**
   * CRUD for platform-global AI system prompt variants.  Superuser-only.  Use the ``activate`` action to promote a variant as the active prompt for its type (retiring the previous active variant).  list: List all prompt variants (superuser only). Supports ?promptType= filter.  retrieve: Return a single variant.  create: Create a new variant (status defaults to \'draft\').  update / partial_update: Edit a variant. Cannot edit an \'active\' variant directly — clone it first, edit the clone, then activate the clone.  delete: Delete a variant. Active variants cannot be deleted.
   */
  async updateRaw(
    requestParameters: UpdateRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<SystemPromptVariant>> {
    if (requestParameters['id'] == null) {
      throw new runtime.RequiredError('id', 'Required parameter "id" was null or undefined when calling update().');
    }

    if (requestParameters['systemPromptVariant'] == null) {
      throw new runtime.RequiredError(
        'systemPromptVariant',
        'Required parameter "systemPromptVariant" was null or undefined when calling update().',
      );
    }

    const queryParameters: any = {};

    const headerParameters: runtime.HTTPHeaders = {};

    headerParameters['Content-Type'] = 'application/json';

    if (
      this.configuration &&
      (this.configuration.username !== undefined || this.configuration.password !== undefined)
    ) {
      headerParameters['Authorization'] =
        'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
    }
    if (this.configuration && this.configuration.apiKey) {
      headerParameters['Authorization'] = await this.configuration.apiKey('Authorization'); // tokenAuth authentication
    }

    if (this.configuration && this.configuration.apiKey) {
      headerParameters['Authorization'] = await this.configuration.apiKey('Authorization'); // courseKeyAuth authentication
    }

    let urlPath = `/promptVariants/{id}/`;
    urlPath = urlPath.replace(`{${'id'}}`, encodeURIComponent(String(requestParameters['id'])));

    const response = await this.request(
      {
        path: urlPath,
        method: 'PUT',
        headers: headerParameters,
        query: queryParameters,
        body: requestParameters['systemPromptVariant'],
      },
      initOverrides,
    );

    return new runtime.JSONApiResponse(response);
  }

  /**
   * CRUD for platform-global AI system prompt variants.  Superuser-only.  Use the ``activate`` action to promote a variant as the active prompt for its type (retiring the previous active variant).  list: List all prompt variants (superuser only). Supports ?promptType= filter.  retrieve: Return a single variant.  create: Create a new variant (status defaults to \'draft\').  update / partial_update: Edit a variant. Cannot edit an \'active\' variant directly — clone it first, edit the clone, then activate the clone.  delete: Delete a variant. Active variants cannot be deleted.
   */
  async update(
    requestParameters: UpdateRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<SystemPromptVariant> {
    const response = await this.updateRaw(requestParameters, initOverrides);
    return await response.value();
  }
}
